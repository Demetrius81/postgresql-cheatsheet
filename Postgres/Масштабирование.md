> **Масштабирование базы данных: вертикальное и горизонтальное. Поиск узких мест и оптимизация запросов (на примере PostgreSQL).**

---

## 🧭 Вступление

Любая система с ростом нагрузки сталкивается с предельными возможностями своей базы данных.
Поначалу хватает одного узла — простая установка PostgreSQL с SSD и 16 ГБ RAM работает быстро.
Но спустя время запросов становится больше, данные растут, и база перестаёт укладываться в ресурсы одного сервера.
Появляются **узкие места** — задержки при записи, блокировки, рост latency на запросах, нехватка памяти под индексы.

Именно в этот момент инженер должен понимать:

* где предел **вертикального масштабирования**,
* когда пора переходить к **горизонтальному**,
* как **найти бутылочные горлышки** и **оптимизировать запросы**, прежде чем «лечить кластером то, что чинится индексом».

---

## ⚙️ Вертикальное масштабирование (scale-up)

### 💡 Суть

Это наращивание мощности **одного узла**:

* больше CPU,
* больше RAM,
* более быстрые SSD / NVMe,
* улучшенные параметры конфигурации PostgreSQL.

### 🔧 Настройки PostgreSQL, которые чаще всего масштабируют:

1. **shared_buffers** — объём памяти под кэш страниц (рекомендуется ~25% RAM).
2. **work_mem** — память на сортировку / агрегацию (важно не переоценить).
3. **maintenance_work_mem** — используется при `VACUUM`, `CREATE INDEX`, `ALTER TABLE`.
4. **effective_cache_size** — подсказка планировщику, сколько данных помещается в ОС-кэш.
5. **max_connections** и `pgbouncer` для connection pooling.
6. **autovacuum** — для предотвращения bloat (разрастания таблиц).

### ✅ Плюсы

* Простота: всё работает на одной ноде.
* Транзакционная целостность и ACID остаются без изменений.
* Минимальные изменения в архитектуре.

### ❌ Минусы

* Есть физический предел (железо не бесконечное).
* Стоимость топового CPU и RAM растёт **экспоненциально**.
* Одна точка отказа (SPOF).
* Проблемы с бэкапами и временем восстановления.

📘 Вертикальное масштабирование — первая стадия роста.
Когда система упрётся в лимит одного сервера (обычно ~1–2 TB данных или ~100k TPS), переходят к **горизонтальному масштабированию**.

---

## ⚙️ Горизонтальное масштабирование (scale-out)

### 💡 Суть

Разделение нагрузки между несколькими узлами:

* **репликация** (чтение/запись разделяются),
* **шардинг** (данные делятся по логическим ключам),
* **федерация / распределённые системы**.

### 🔹 Репликация (Master → Read Replica)

PostgreSQL поддерживает **streaming replication**:

```bash
wal_level = replica
max_wal_senders = 5
```

Обычно:

* `master` принимает запись,
* `replicas` обслуживают чтение.

Запросы делятся:

* INSERT/UPDATE/DELETE → master
* SELECT → replica (через балансировщик, например, `pgpool-II` или `HAProxy`).

⚠️ Проблема — **лаг репликации**.
Если на replica читается почти в реальном времени, возможна ситуация "прочитал до коммита".

---

### 🔹 Шардинг

Данные разделяются на несколько нод:

* **по пользователю** (`user_id % N`),
* **по географии** (EU / US),
* **по типу данных**.

Пример:

```text
user_id 1–1_000_000 → shard_1
user_id 1_000_001–2_000_000 → shard_2
```

Инструменты:

* **Citus** (PostgreSQL extension, превращает PG в распределённую СУБД),
* **CockroachDB**, **YugabyteDB** — совместимы с PostgreSQL-протоколом.

📈 Это масштабируется почти линейно по узлам.
Но есть сложности с транзакциями (особенно cross-shard), уникальными ключами и joins.

---

### 🔹 Federation

Каждая база обслуживает свой сервис / bounded context (DDD).
Коммуникация идёт через API, очереди (Kafka, RabbitMQ), event sourcing.
Это не столько масштабирование БД, сколько **архитектурная декомпозиция**.

---

## 🧩 Поиск узких мест

Прежде чем масштабировать, нужно **понять, что именно тормозит**.
Иногда «нагрузка» — это всего лишь неоптимичный запрос.

### 🔍 Методы диагностики

#### 1. `EXPLAIN (ANALYZE, BUFFERS)`

Показывает реальный план выполнения:

```sql
EXPLAIN (ANALYZE, BUFFERS)
SELECT * FROM orders WHERE customer_id = 123;
```

📊 Вы увидите:

* какие индексы используются,
* где выполняется seq scan,
* сколько страниц было прочитано из диска или кэша.

#### 2. `pg_stat_statements`

Включается в `postgresql.conf`:

```bash
shared_preload_libraries = 'pg_stat_statements'
```

Показывает:

* самые медленные запросы,
* частоту вызовов,
* среднее время выполнения.

#### 3. `auto_explain`

Логирует долгие запросы (>100ms) в `postgres.log`.

#### 4. `pg_top`, `iostat`, `vmstat`

Для анализа CPU, I/O, блокировок.

---

## ⚡ Оптимизация запросов и структуры данных

### 1. Индексы

* Используй `btree` для диапазонов, `GIN` для JSONB, `GiST` для геоданных.
* Не делай избыточные индексы — они замедляют вставки.
* Комбинированные индексы должны соответствовать фильтру в WHERE по порядку.

```sql
CREATE INDEX idx_orders_customer_date ON orders(customer_id, order_date DESC);
```

### 2. Денормализация (осознанно)

Иногда эффективнее хранить вычисленные данные:

```sql
ALTER TABLE orders ADD COLUMN total NUMERIC GENERATED ALWAYS AS (price * quantity) STORED;
```

### 3. Партиционирование

Для больших таблиц (логов, историй):

```sql
CREATE TABLE logs (
  id SERIAL, created_at DATE, message TEXT
) PARTITION BY RANGE (created_at);
```

Каждая партиция хранится отдельно → меньше данных при сканировании.

### 4. Кэширование

* Redis для горячих данных.
* Materialized view в PostgreSQL.
* Query caching на уровне приложения.

---

## 🧠 Заключение

Масштабирование базы данных — это **не только про добавление серверов**, а про понимание баланса между архитектурой, оптимизацией и экономикой.
Настоящие узкие места обычно находятся **в неиндексированных запросах, неаккуратных join-ах и неосознанных транзакциях**, а не в «малом количестве CPU».

Идеальный порядок действий:

1. 🔍 Оптимизировать запросы (`EXPLAIN`, `pg_stat_statements`);
2. 🧩 Настроить кеши, индексы, партиции;
3. 💾 Вертикально масштабировать PostgreSQL;
4. 🧭 Внедрить реплики;
5. ⚙️ Переходить на шардинг / распределённые системы при росте данных.

---
