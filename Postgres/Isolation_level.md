
## 🔷 Введение

**Изоляция транзакций** — это часть механизма **ACID** (Atomicity, Consistency, Isolation, Durability), определяющая, насколько результаты одной транзакции «видят» другие параллельно выполняющиеся транзакции.
PostgreSQL реализует это через **MVCC** — *Multiversion Concurrency Control* (многоверсионный контроль конкурентности), что позволяет читать данные без блокировок, создавая разные «версии» строк для разных транзакций.

---

## 🔹 Уровни изоляции в PostgreSQL (4 уровня по стандарту SQL)

PostgreSQL поддерживает **все четыре уровня**, описанных в стандарте SQL-92:

| Уровень              | Гарантирует защиту от                               | Особенности                                                                     |
| -------------------- | --------------------------------------------------- | ------------------------------------------------------------------------------- |
| **Read Uncommitted** | ничего (PostgreSQL обрабатывает как Read Committed) | По сути эквивалентен `Read Committed`                                           |
| **Read Committed**   | Dirty Read                                          | Каждое `SELECT` видит только зафиксированные данные на момент своего выполнения |
| **Repeatable Read**  | Dirty Read, Non-repeatable Read                     | Все запросы внутри транзакции видят *один и тот же снимок* данных               |
| **Serializable**     | Dirty Read, Non-repeatable Read, Phantom Read       | Полная сериализация — транзакции ведут себя так, будто выполняются по очереди   |

---

## ⚙️ Подробно о каждом уровне

### 🟢 1. Read Uncommitted

* Формально разрешает «грязные чтения» (чтение незакоммиченных данных),
  но в **PostgreSQL** фактически **отсутствует** — он работает как `Read Committed`.
* То есть PostgreSQL **никогда не покажет незакоммиченные изменения**.

💡 **Итог**: безопасен, но это не «реальный» Read Uncommitted.

---

### 🟡 2. Read Committed — уровень по умолчанию

* Каждый SQL-запрос внутри транзакции видит только те данные,
  которые **были зафиксированы (committed)** до начала запроса.
* Если в середине транзакции другой клиент изменит данные и закоммитит —
  следующий `SELECT` в вашей транзакции уже увидит обновлённые значения.

📘 **Пример:**

```sql
BEGIN;
SELECT balance FROM accounts WHERE id = 1;  -- balance = 100
-- В это время другой клиент делает UPDATE accounts SET balance = 200;
SELECT balance FROM accounts WHERE id = 1;  -- balance = 200
COMMIT;
```

→ На втором запросе видим новые данные.
То есть изоляция на уровне **запроса**, а не всей транзакции.

⚠️ **Проблема:** возможны *non-repeatable reads* и *phantom reads*.

---

### 🔵 3. Repeatable Read

* Все `SELECT` внутри одной транзакции видят **один и тот же снимок данных**,
  существовавший на момент **начала транзакции**.
* Даже если другие транзакции внесли изменения и закоммитили —
  ваш контекст остаётся неизменным.

📘 **Пример:**

```sql
BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ;
SELECT COUNT(*) FROM orders; -- 100
-- другой клиент вставляет новую запись
SELECT COUNT(*) FROM orders; -- всё ещё 100
COMMIT;
```

⚙️ PostgreSQL при этом **разрешает фантомы только логически невозможные** —
на практике поведение ближе к «Snapshot Isolation» (синоним MVCC-снимка).

⚠️ **Подводный камень:** возможна *serialization anomaly* —
две транзакции могут «честно» считать данные и обе закоммитить,
создав логически неконсистентное состояние.

---

### 🔴 4. Serializable

* Самый строгий уровень. PostgreSQL использует **Serializable Snapshot Isolation (SSI)**.
* Система отслеживает зависимости между транзакциями,
  и если обнаруживает потенциальный конфликт, **одна транзакция откатывается** (`ROLLBACK`).

📘 **Пример:**

```sql
BEGIN TRANSACTION ISOLATION LEVEL SERIALIZABLE;
SELECT SUM(balance) FROM accounts; -- 1000
-- другая транзакция тоже считает и переводит деньги
-- обе завершаются, но система видит логический конфликт → одна откатится
COMMIT;
```

💡 **Итог:** полная эквивалентность последовательному выполнению транзакций.
Но при высокой конкуренции — возможны частые автоматические откаты.

---

## 🔍 MVCC — как PostgreSQL реализует изоляцию

* Каждая запись в таблице имеет два «системных» поля:

  * `xmin` — ID транзакции, которая создала строку
  * `xmax` — ID транзакции, которая удалила (или изменила) строку
* Когда транзакция делает `SELECT`, она видит только те строки,
  где `xmin` ≤ текущая транзакция, и `xmax` неактивен.
* При `UPDATE` создаётся **новая версия строки**, а старая остаётся до завершения других транзакций.

💡 Это позволяет **читать без блокировок** даже при активной записи.

---

## ⚠️ Подводные камни

1. **Serializable может вызывать неожиданные откаты.**
   Нужно уметь перезапускать транзакцию при `serialization failure`.

2. **Repeatable Read может давать устаревшие данные.**
   Например, если вы проверили условие и потом записали —
   оно уже могло стать неверным в «реальности».

3. **Read Committed не гарантирует согласованности между несколькими запросами.**
   Особенно опасно в отчётных или финансовых вычислениях.

4. **MVCC вызывает рост "мертвых" строк (dead tuples)**,
   поэтому важно регулярно выполнять **VACUUM** и **ANALYZE**.

---

## 🧩 Заключение

Изоляция в PostgreSQL — не просто флаг в `BEGIN TRANSACTION`,
а мощный механизм, определяющий поведение системы под нагрузкой.

* Используйте `READ COMMITTED` по умолчанию — он быстр и безопасен для большинства CRUD-операций.
* Применяйте `REPEATABLE READ`, если нужны стабильные данные внутри транзакции.
* Включайте `SERIALIZABLE`, если важна **логическая целостность** и вы готовы обрабатывать возможные откаты.

И помните: чем выше изоляция, тем ниже конкурентность и выше накладные расходы —
всегда балансируйте между **точностью** и **производительностью**.

---

